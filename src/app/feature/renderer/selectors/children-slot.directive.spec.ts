import { ChildrenSlotDirective } from './children-slot.directive';

describe('ChildrenSlotDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildrenSlotDirective();
    expect(directive).toBeTruthy();
  });
});
